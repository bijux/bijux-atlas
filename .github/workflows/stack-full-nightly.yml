name: stack-full-nightly

on:
  schedule:
    - cron: '20 2 * * *'
  workflow_dispatch: {}

concurrency:
  group: stack-full-nightly
  cancel-in-progress: false

jobs:
  stack-full:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      ATLAS_PERF_BASELINE_PROFILE: ci-runner
      ATLAS_E2E_ENABLE_OTEL: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.cache/kind
          key: kind-cache-${{ runner.os }}-v0.31.0
      - uses: azure/setup-helm@v4
      - uses: azure/setup-kubectl@v4
      - uses: grafana/setup-k6-action@v1
      - run: make ci-ops-install-prereqs
      - run: make stack-full
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stack-full-nightly-artifacts
          path: |
            artifacts/stack-report/**
            artifacts/ops/**
