name: security-supply-chain-validation

on:
  push:
    branches: [main]
  pull_request:

jobs:
  validate-supply-chain:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: setup-rust
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7

      - name: generate governance evidence required by security policy
        run: |
          cargo run --locked -q -p bijux-dev-atlas -- governance exceptions validate --format json || true
          cargo run --locked -q -p bijux-dev-atlas -- governance deprecations validate --format json || true
          cargo run --locked -q -p bijux-dev-atlas -- governance breaking validate --format json || true
          cargo run --locked -q -p bijux-dev-atlas -- governance doctor --format json || true

      - name: run dependency security validation
        run: cargo run --locked -q -p bijux-dev-atlas -- security validate --format json

      - name: run dependency audit command
        run: cargo run --locked -q -p bijux-dev-atlas -- security dependency-audit --format json

      - name: run supply chain contracts
        run: cargo test --locked -p bijux-dev-atlas --test security_supply_chain_contracts -- --nocapture
