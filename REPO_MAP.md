# Repository Map

- `crates/` runtime and control-plane Rust crates. Owner: `@bijan`.
- `configs/` configuration and schema SSOT. Owner: `@bijan`.
- `docs/` user and internal documentation. Owner: `@bijan`.
- `docker/` container image definitions and contracts. Owner: `@bijan`.
- `make/` thin entrypoint wrappers for control-plane commands. Owner: `@bijan`.
- `ops/` operational contracts, inventory, manifests, and policy inputs. Owner: `@bijan`.
- `.github/` CI workflows and automation policy. Owner: `@bijan`.
- `artifacts/` generated runtime outputs (never tracked). Owner: N/A.
