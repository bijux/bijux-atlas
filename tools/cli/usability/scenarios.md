# CLI Usability Scenarios

1. New user discovers command domains using `discover_subcommands.py`.
2. Operator enables shell completion and confirms command suggestions.
3. CI engineer runs config-aware wrapper with env overrides.
4. Developer uses interactive help to inspect command surfaces.
5. Automation script consumes JSON telemetry and audit output.
