# Ops Checks Contract

- Ops checks use canonical IDs: `checks_ops_<area>_<name>`.
- Ops suites may only include `checks_ops_*` unless explicitly allowlisted.
- Every check must declare owner, severity, slow flag, and fix hint.
