> Redirect Notice: canonical handbook content lives under `docs/operations/` (see `docs/operations/ops-system/INDEX.md`).

# E2E Philosophy

- Owner: bijux-atlas-operations
- Stability: stable

E2E coverage verifies contract behavior across stack boundaries with deterministic fixtures first, then integration lanes.
