# Ingest Benchmark Summary

Ingest benchmark coverage includes:

- scale scenarios: small, medium, large, sharded, concurrent
- overhead checks: anomaly, validation, hashing, shard generation, catalog generation
- scaling checks: parallelism and thread pool
- artifact checks: size and compression
- regression fixtures and CI lane
