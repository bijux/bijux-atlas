# Navigation Contract

Navigation is defined only in `mkdocs.yml`.

- Top-level navigation labels are fixed: Home, Start Here, Product, Architecture, Operations, Development, Reference
- Top-level navigation order is fixed: Home -> Start Here -> Product -> Architecture -> Operations -> Development -> Reference
