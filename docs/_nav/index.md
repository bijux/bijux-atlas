# Navigation Contract

Navigation is defined only in `mkdocs.yml`.

- Top-level navigation labels are fixed: Start Here, Product, Architecture, API, Operations, Development, Reference
- Top-level navigation order is fixed: Start Here -> Product -> Architecture -> API -> Operations -> Development -> Reference
