# Docs Checks Contract

- Docs checks use canonical IDs: `checks_docs_<area>_<name>`.
- Docs suites may only include `checks_docs_*` unless explicitly allowlisted.
- All docs checks must expose deterministic output for CI and local runs.
