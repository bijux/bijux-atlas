# Developer Workflow Quickstart

- Owner: `docs-governance`
- Stability: `stable`

Baseline contributor loop:

```bash
make fmt
make lint
make test
make check
```

For docs/configs/ops workflows, use `bijux dev atlas ...` command families through make wrappers.
