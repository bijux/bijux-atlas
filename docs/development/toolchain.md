# Toolchain

Owner: `platform`  
Type: `guide`  
Audience: `contributor`  
Reason to exist: define canonical contributor tooling requirements.

## Core Tools

- Rust toolchain from `rust-toolchain.toml`.
- Make targets for local validation.
- `bijux dev atlas` for repository governance and contract checks.
