# Atlasctl Deletion

Cutoff date: 2026-03-31

This directory is the only allowed location for historical `atlasctl` references.

Rules:
- No new `atlasctl` references may be introduced outside this directory.
- Historical notes only; no active workflow or operator guidance.
- Active control-plane commands must use `bijux dev atlas ...`.
