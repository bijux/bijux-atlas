# Ops Command Inventory

Canonical ops command inventory snapshot lives in:

- `crates/bijux-dev-atlas/docs/OPS_COMMAND_LIST.md`

To inspect the live command surface locally:

```bash
cargo run -p bijux-dev-atlas -- ops --help
```
