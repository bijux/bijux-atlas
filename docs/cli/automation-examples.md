# CLI Automation Examples

```bash
bijux-dev-atlas api list --output-format json > /tmp/api-list.json
bijux-dev-atlas observe metrics list --output-format json > /tmp/metrics.json
bijux-dev-atlas help --format json > /tmp/cli-groups.json
```
