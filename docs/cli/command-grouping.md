# CLI Command Grouping by Domain

- `integration`: `api`
- `operations`: `ops`, `observe`, `load`, `audit`
- `integrity`: `invariants`, `drift`, `reproduce`, `governance`, `system`
- `verification`: `check`, `checks`, `contract`, `registry`, `suites`, `tests`

Generate live grouped snapshot:

```bash
bijux-dev-atlas help --format json
```
