# CLI Error Message Conventions

- Prefix with surface (`cli.config`, `cli.runtime`, `cli.validation`).
- State the failing input and expected shape.
- Provide one concrete remediation step.
- Preserve underlying error detail for debugging.
