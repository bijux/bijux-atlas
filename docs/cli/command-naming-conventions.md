# CLI Command Naming Conventions

- Use domain-first groups (`ops`, `api`, `observe`, `load`).
- Use verb-oriented leaf commands (`list`, `explain`, `verify`, `run`).
- Keep command names lowercase and hyphen-safe.
- Prefer stable nouns over temporary labels.
