# CLI Output Format Standards

- Human output for interactive use.
- JSON output for automation (`--output-format json`).
- Deterministic field order in JSON payloads where practical.
- Non-zero exit with explicit error classification in stderr.
