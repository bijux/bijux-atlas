# Policies Checks Contract

- Policy checks use canonical IDs: `checks_policies_<area>_<name>` or domain-equivalent canonical IDs.
- New checks must have owner metadata and a docs reference.
- Deprecated marketing-tier tags are forbidden in check metadata.
