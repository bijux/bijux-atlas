# Performance Baseline Metrics

Baseline snapshots must include:

- p50, p95, p99 latency
- throughput (`requests_per_second`)
- ingest throughput (`ingest_ops_per_second`)
- CPU utilization
- memory usage
- error rate
