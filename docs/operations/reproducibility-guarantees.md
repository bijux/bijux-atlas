# Reproducibility Guarantees

Atlas reproducibility surfaces guarantee:

1. Deterministic reproducibility scenario catalog ordering.
2. Deterministic `reproduce run` report structure.
3. Stable required scenario set verification in `reproduce verify`.
4. Offline-safe execution contract (`environment.offline_safe = true`).
