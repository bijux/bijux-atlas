# Metric Labels

Required baseline labels:

- `subsystem`
- `version`
- `dataset`

Route and request metrics can include:

- `route`
- `method`
- `status`
- `class`

Rule: label values must be bounded enums or normalized identifiers.
