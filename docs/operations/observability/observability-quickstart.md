# Observability Quickstart

1. Start Atlas with metrics and tracing enabled.
2. Confirm `/metrics`, `/healthz`, and `/readyz` responses.
3. Capture `system debug diagnostics` output.
4. Verify alert rules and dashboard data sources.
5. Run one smoke query and confirm logs and traces are correlated.
