# Alerting Configuration Guide

Alerting baseline:

- error ratio
- latency percentile
- readiness failure
- overload persistence
- slow query rate

Policy:

- Every critical alert must map to a runbook.
- Use deterministic labels for route, severity, and owner.
