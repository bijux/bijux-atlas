# Reproducibility Status Summary

Use:

```bash
bijux-dev-atlas reproduce status --format json
```

The status summary combines:

1. Latest `reproduce verify` contract status.
2. Presence of reproducibility evidence bundle report.
3. Classification of verification failures when present.
