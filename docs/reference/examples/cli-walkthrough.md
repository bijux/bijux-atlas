# CLI Walkthrough

- Owner: `docs-governance`
- Audience: `user`
- Type: `guide`
- Stability: `stable`
- Reason to exist: provide a step-by-step CLI workflow for local validation.

## Walkthrough

1. Run checks:

```bash
make check-all
```

2. Run contracts:

```bash
make contract-all
```

3. Run tests:

```bash
cargo nextest run
```
