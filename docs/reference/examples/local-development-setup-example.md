# Local Development Setup Example

- Owner: `docs-governance`
- Audience: `contributor`
- Type: `guide`
- Stability: `stable`
- Reason to exist: provide a concise local setup checklist.

## Setup checklist

1. Install Rust toolchain and make.
2. Run `cargo nextest run`.
3. Run `make check-all` and `make contract-all`.
4. Run docs link validation.
