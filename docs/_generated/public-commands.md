# Public Command List

Generated from atlasctl command registry.

- `check`: run registered check suites and individual checks (owner: `platform`)
- `configs`: configs checks and inventories (owner: `platform`)
- `contracts`: contracts domain commands (owner: `platform`)
- `dev`: dev control-plane group command surface (owner: `platform`)
- `docs`: docs checks and generation (owner: `docs`)
- `doctor`: show tooling and context diagnostics (owner: `platform`)
- `gates`: run gate contracts and lane checks (owner: `ops`)
- `install`: installation checks and doctor commands (owner: `platform`)
- `inventory`: generate and verify inventories (owner: `repo`)
- `k8s`: k8s checks and suites (owner: `ops`)
- `layout`: layout domain commands (owner: `repo`)
- `lint`: lint suite runner (owner: `platform`)
- `list`: list checks/commands/suites from canonical registries (owner: `platform`)
- `load`: load and perf suites (owner: `ops`)
- `make`: make target wrappers and make contract tooling (owner: `platform`)
- `obs`: observability checks and drills (owner: `ops`)
- `ops`: ops checks and suite orchestration (owner: `ops`)
- `owners`: owner id registry and validation commands (owner: `platform`)
- `packages`: package domain commands (owner: `platform`)
- `policies`: policy relaxations and bypass checks (owner: `platform`)
- `registry`: registry domain commands (owner: `platform`)
- `release`: release readiness checklist commands (owner: `platform`)
- `repo`: repository stats and density reports (owner: `repo`)
- `report`: unified report and scorecard commands (owner: `platform`)
- `run-id`: generate and print run identifiers (owner: `platform`)
- `stack`: stack lifecycle and checks (owner: `ops`)
- `suite`: atlasctl-native suite runner (owner: `platform`)
- `test`: run canonical atlasctl test suites (owner: `platform`)
