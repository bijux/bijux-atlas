# v0.1.0 Ops Readiness Checklist

1. Run `make ops-doctor` and capture unresolved violations.
2. Run `make ops-validate` and confirm all governance checks pass.
3. Run `make ops-pins-check` and confirm `ops/inventory/pin-freeze.json` is still `state: frozen`.
4. Regenerate curated evidence artifacts and commit updates under `ops/_generated.example/`.
5. Verify `ops/_generated.example/ops-evidence-bundle.json` inventory and schema hashes match current files.
6. Verify `ops/_generated.example/scorecard.json` status is release-acceptable for v0.1.0.
7. Verify fixture governance artifacts are current:
   - `ops/datasets/generated/fixture-inventory.json`
   - `ops/_generated.example/fixture-drift-report.json`
8. Confirm `ops/CONTROL_PLANE.md` matches `ops/_generated.example/control-plane.snapshot.md`.
9. Confirm all root ops docs are linked from `ops/INDEX.md`.
10. Tag release only after all previous checks are green and committed.
