{
  "failure_exit_code": 2,
  "kind": "performance_regression_ci_contract",
  "required_commands": [
    "bijux-atlas-dev load baseline --format json",
    "bijux-atlas-dev load run --format json",
    "bijux-atlas-dev load compare --format json"
  ],
  "schema_version": 1
}
