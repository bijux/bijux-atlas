# ops/_generated.example

Curated generated evidence snapshots committed for contract verification.

- Update only through `bijux dev atlas` generators.
- Keep content deterministic and reviewable.
- Mirror policy and the committed artifact whitelist are declared in `ops/_generated.example/MIRROR_POLICY.md` and `ops/_generated.example/ALLOWLIST.json`.
- `ops-index.json` is the curated ops index artifact.
- `ops-evidence-bundle.json` is the curated release evidence summary.
