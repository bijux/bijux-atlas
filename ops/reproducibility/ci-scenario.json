{
  "schema_version": 1,
  "scenario_id": "reproducibility-ci",
  "description": "Runs reproducibility verification in CI with deterministic JSON outputs.",
  "steps": [
    "bijux-atlas-dev reproduce run --format json",
    "bijux-atlas-dev reproduce verify --format json",
    "bijux-atlas-dev reproduce lineage-validate --format json",
    "bijux-atlas-dev reproduce audit-report --format json"
  ],
  "required_artifacts": [
    "artifacts/reproducibility/run-report.json",
    "artifacts/reproducibility/audit-report.json",
    "artifacts/reproducibility/metrics.json",
    "artifacts/reproducibility/summary-table.md"
  ]
}
