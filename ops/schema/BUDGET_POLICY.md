# Schema Budget Policy

- Owner: `bijux-atlas-operations`
- Purpose: keep ops schema growth intentional and reviewable

The canonical schema inventory is governed by `ops/schema/generated/schema-index.json`.
Growth beyond the baseline budget requires an explicit entry in `ops/schema/SCHEMA_BUDGET_EXCEPTIONS.md`.
