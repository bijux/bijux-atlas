# Render Pipeline

- Owner: bijux-atlas-operations
- Stability: stable

Render pipeline generates deterministic manifests and hash indexes.

- `bijux dev atlas ops render --profile <name>`
- `bijux dev atlas ops render --check`
