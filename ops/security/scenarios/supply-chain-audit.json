{
  "schema_version": 1,
  "kind": "supply_chain_audit_scenario",
  "steps": [
    "cargo run -q -p bijux-atlas-dev -- security validate --format json",
    "cargo run -q -p bijux-atlas-dev -- security dependency-audit --format json"
  ],
  "status": "ready"
}
