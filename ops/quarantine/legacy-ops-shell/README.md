# Legacy Ops Shell Quarantine

This directory is reserved for temporary containment of legacy ops shell helpers during migration.

Rules:
- New files are forbidden.
- Existing legacy scripts must be removed after parity is reached in `bijux dev atlas ops`.
- This directory must remain empty except for `README.md` and optional `.gitkeep`.

Deadline:
- Remove all legacy shell helpers by 2026-06-30.
