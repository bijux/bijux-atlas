# Release Directory

This directory holds checked-in release-facing material that is governed at the
repository root. Keep durable release records and supporting checked-in assets
here rather than scattering them across unrelated directories.
