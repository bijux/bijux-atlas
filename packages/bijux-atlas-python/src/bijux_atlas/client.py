"""High-level SDK client entrypoint."""


class BijuxAtlasClient:
    """Client placeholder until endpoint bindings are migrated."""

    def __init__(self, base_url: str) -> None:
        self.base_url = base_url
