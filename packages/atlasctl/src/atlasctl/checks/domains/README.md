# Checks Domains

`atlasctl.checks.domains` is the canonical home for check implementations.

Domain set:
- `repo`
- `ops`
- `policies`
- `docs`
- `configs`
- `internal`

`dev` checks are treated as `internal` for ownership and visibility unless explicitly promoted to user-facing checks.
