## Observability Checks

Observability check implementations are canonical under `atlasctl/observability/contracts/*`.

This directory is intentionally documentation-only to avoid duplicate check
trees under both `atlasctl/checks/*` and `atlasctl/observability/*`.
