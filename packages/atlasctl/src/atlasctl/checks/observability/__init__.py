from __future__ import annotations

# Canonical observability check domain placeholder.
# Existing observability contracts are still being migrated under atlasctl/checks/.
CHECKS: tuple[object, ...] = ()
