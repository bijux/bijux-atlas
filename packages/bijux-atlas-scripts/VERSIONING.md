# Versioning Strategy

`bijux-atlas-scripts` follows semantic versioning.

- `MAJOR`: breaking command or output-contract change.
- `MINOR`: additive commands, flags, or schema fields.
- `PATCH`: fixes and non-breaking internal refactors.

Current package version source of truth is `packages/bijux-atlas-scripts/pyproject.toml`.
