# Boundary

`bijux-atlas-py` is user-facing library surface.

Rules:
- No imports from `bijux-atlas-scripts` package code.
- No operational CLI logic embedded here.
- Use stable file/API contracts as integration boundary.
