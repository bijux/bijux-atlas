# Gate configs

- Owner: `platform`
- Purpose: declare CI and release gate lane inputs.
- Consumers: gate orchestration inside `bijux-dev-atlas` and CI workflows.
- Update workflow: edit the lane manifests, then rerun the gate and required-suite checks.
