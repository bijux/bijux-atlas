# Shellcheck configs

- Owner: `platform`
- Purpose: configure shell lint behavior for maintained shell surfaces.
- Consumers: shell lint checks and CI lint lanes.
- Update workflow: change the lint config together with shell surface updates, then rerun shell lint validation.
