schema_version: 1
assignments:
  - principal: user
    role_id: role.user.readonly
  - principal: service-account
    role_id: role.service.readonly
  - principal: operator
    role_id: role.operator.admin
  - principal: ci
    role_id: role.automation.release
