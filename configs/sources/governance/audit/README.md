# Audit Configurations

Canonical audit bundle and compliance matrix configuration surfaces consumed by the control-plane audit commands.

Rules:
- Authored audit inputs stay in this directory root.
- Validation schemas for audit inputs live under `schemas/`.
