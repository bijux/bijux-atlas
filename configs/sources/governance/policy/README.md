# Policy Configs

- Owner: `atlas-governance`
- Purpose: define approved exceptions, ratchets, and enforcement policies for repository controls.
- Consumers: policy-aware validation and governance enforcement inside `bijux-dev-atlas`.
- Update workflow: edit the relevant policy artifact with approval context, then rerun the matching validators.
- Boundary: authored policy inputs stay at this directory root, and local schemas live under `schemas/`.
