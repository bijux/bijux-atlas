{
  "schema_version": 1,
  "membership_boundary": "Each governed entry should have one primary suite membership that matches its real purpose.",
  "allowed_overlap": "Overlap is only allowed when one governed entry must satisfy two distinct review surfaces and duplicating the implementation would reduce truthfulness.",
  "move_procedure": [
    "Update the owning registry first.",
    "Update the suite registry next.",
    "Update any make or workflow wrapper that exposes the entry.",
    "Rerun the affected suite and registry validation."
  ]
}
