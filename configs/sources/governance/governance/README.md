# Governance Configs

Machine-readable governance policy for suites, registries, compatibility, deprecations, and exceptions.

Rules:
- Authored governance inputs stay in this directory root.
- Local schemas for governance inputs live under `schemas/` and `enforcement/schemas/`.
