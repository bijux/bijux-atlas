# Clients Configs

Machine-readable client documentation registry and schema bindings.

Rules:
- Authored client inputs stay at this directory root.
- Local schemas for client inputs live under `schemas/`.
