# CLI Configs

This directory contains governed CLI registry inputs consumed by `bijux dev atlas` help and doctor surfaces.

Rules:
- Authored CLI inputs stay at this directory root.
- Local schemas for CLI inputs live under `schemas/`.
