# System Simulation Configurations

Canonical simulation scenarios, failure injections, and report schema surfaces consumed by the control-plane system simulation commands.

Rules:
- Authored system simulation inputs stay at this directory root.
- Local schemas for system report surfaces live under `schemas/`.
