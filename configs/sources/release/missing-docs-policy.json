{
  "schema_version": 1,
  "crate_policy": {
    "bijux-atlas": {
      "enforce_deny_missing_docs": false,
      "exception": "the compatibility alias crate inherits most reader guidance from the canonical runtime and split crate handbooks"
    },
    "bijux-atlas-api": {
      "enforce_deny_missing_docs": false,
      "exception": "the API contract crate currently splits reader-facing guidance across the repository runtime, interface, and contract handbooks"
    },
    "bijux-atlas-cli": {
      "enforce_deny_missing_docs": false,
      "exception": "the CLI crate is documented through the repository runtime and workflow handbooks while crate-local rustdoc coverage is still narrow"
    },
    "bijux-atlas-core": {
      "enforce_deny_missing_docs": false,
      "exception": "the core crate is intentionally small and defers narrative coverage to the shared foundations handbook"
    },
    "bijux-atlas-ingest": {
      "enforce_deny_missing_docs": false,
      "exception": "the ingest crate relies on the repository ingest and runtime handbooks for end-to-end workflow explanation"
    },
    "bijux-atlas-model": {
      "enforce_deny_missing_docs": false,
      "exception": "the model crate surfaces stable types but the explanatory guidance still lives in the shared foundations handbook"
    },
    "bijux-atlas-ops": {
      "enforce_deny_missing_docs": false,
      "exception": "the ops crate is published for operational contracts, but most human-facing guidance still lives under the operations handbook"
    },
    "bijux-atlas-query": {
      "enforce_deny_missing_docs": false,
      "exception": "the query crate is documented through the shared runtime and workflow pages while crate-local rustdoc remains concise"
    },
    "bijux-atlas-runtime": {
      "enforce_deny_missing_docs": false,
      "exception": "the runtime crate remains the orchestration owner, but its reader-facing guidance is intentionally split across repository handbooks"
    },
    "bijux-atlas-server": {
      "enforce_deny_missing_docs": false,
      "exception": "the server crate depends on the repository runtime and operations handbooks for deployment and HTTP narrative coverage"
    },
    "bijux-atlas-store": {
      "enforce_deny_missing_docs": false,
      "exception": "the store crate is documented through the repository runtime and contract pages while crate-local rustdoc stays implementation-focused"
    }
  }
}
