# Metadata configs

- Owner: `platform`
- Purpose: store internal ownership and metadata inputs that support registry alignment.
- Consumers: ownership and authority checks inside `bijux-dev-atlas`.
- Update workflow: edit metadata with the related ownership change, then rerun configs authority contracts.
