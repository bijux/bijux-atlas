# Report Registry Schemas

Schemas in this directory validate registry metadata for governed reports.

Surface:
- `reports.registry.schema.json` and `schema-registry.schema.json` validate the report registry itself.
- `check-report-map.schema.json`, `ownership.schema.json`, and `report-budget.schema.json` validate report governance metadata.
