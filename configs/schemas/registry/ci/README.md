# CI Registry Schemas

Schemas in this directory validate CI-owned registry data such as workflow allowlists, lane surfaces, and policy exceptions.

Boundary:
- CI configuration inputs live under `configs/sources/repository/ci/`.
- CI registry schemas stay here because they validate registry-owned metadata.
