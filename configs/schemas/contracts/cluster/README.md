# Cluster Schemas

Schemas in this directory validate cluster topology and shard layout inputs.

Surface:
- `cluster-config.schema.json` for cluster-wide configuration.
- `node-config.schema.json` for node-level configuration.
- `shard-metadata.schema.json` for shard placement metadata.
