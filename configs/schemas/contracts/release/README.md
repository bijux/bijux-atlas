# Release Schemas

Schemas in this directory validate release manifests, provenance bundles, signing policy, and verification outputs.

Boundary:
- Authored release policy lives under `configs/sources/release/`.
- Release evidence and packet schemas stay here as validation contracts.
