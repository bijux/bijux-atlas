# Performance Schemas

Schemas in this directory validate performance budgets, benchmark inputs, benchmark results, and load reports.

Rules:
- Authored performance inputs live under `configs/sources/operations/perf/`.
- Performance result and report schemas stay here because they validate governed machine outputs.
