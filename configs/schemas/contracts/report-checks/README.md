# Report Check Schemas

Schemas in this directory validate individual check records that appear inside governed reports.

Surface:
- `check-*.schema.json` files validate one report check family each.
- `schema-index.json` is the local index for this check-schema set.
