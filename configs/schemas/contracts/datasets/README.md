# Dataset Schemas

Schemas in this directory validate dataset manifests and ingest workflow records.

Surface:
- `manifest.schema.json` and `pinned-policy.schema.json` validate authored dataset inputs.
- `ingest-plan.schema.json`, `ingest-run.schema.json`, and `endtoend.schema.json` validate ingest artifacts.
