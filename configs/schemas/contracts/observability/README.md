# Observability Schemas

Schemas in this directory validate observability contracts and observability-owned config inputs.

Surface:
- `log.schema.json` and `metrics.schema.json` validate runtime observability outputs.
- `error-codes.schema.json` and `label-policy.schema.json` validate authored observability inputs in `configs/sources/operations/observability/`.
