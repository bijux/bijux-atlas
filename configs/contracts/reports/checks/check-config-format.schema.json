{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bijux.dev/schema/reports/checks/check-config-format.schema.json",
  "title": "Config Format Check Report",
  "type": "object",
  "additionalProperties": true,
  "required": ["report_id", "version", "inputs"],
  "properties": {
    "report_id": { "type": "string", "const": "check-config-format" },
    "version": { "type": "integer", "const": 1 },
    "inputs": { "type": "object" }
  }
}
