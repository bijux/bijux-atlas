{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bijux.dev/schema/reports/checks/check-kubeconform.schema.json",
  "title": "Kubeconform Check Report",
  "type": "object",
  "additionalProperties": true,
  "required": ["report_id", "version", "inputs"],
  "properties": {
    "report_id": { "type": "string", "const": "check-kubeconform" },
    "version": { "type": "integer", "const": 1 },
    "inputs": { "type": "object" }
  }
}
