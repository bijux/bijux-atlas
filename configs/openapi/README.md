# OpenAPI configs

- Owner: `platform`
- Purpose: keep committed OpenAPI snapshots and generated API contract surfaces.
- Consumers: API reference generation, contract checks, and compatibility review.
- Update workflow: regenerate snapshots from the canonical producer, then rerun API and schema validation.
