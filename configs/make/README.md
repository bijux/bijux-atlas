# Make configs

- Owner: `platform`
- Purpose: declare public targets, retired target mappings, and make-surface metadata.
- Consumers: `bijux-dev-atlas` make contracts and help generation.
- Update workflow: change the target metadata with the matching Makefile update, then rerun make contracts.
