# Config Naming Rules

- File names use lowercase `kebab-case`.
- Allowed config suffixes are `.json`, `.toml`, `.yaml`, `.yml`, `.md`, `.txt`.
- `README.md` and `INDEX.md` are reserved documentation names.
- Generated config artifacts belong only under `configs/_generated/`.
- Root-level config files are not allowed unless explicitly listed in `configs/CONTRACT.md`.
