# schema_version = 1
# owner = bijux-atlas-infra
# purpose = Contract config for configs/nextest/nextest.toml
# authority = bijux-atlas-infra
# stability = stable
# last_updated = 2026-02-16

[profile.default]
retries = { count = 1, backoff = "fixed", delay = "1s" }
fail-fast = false
failure-output = "immediate"
success-output = "never"
status-level = "slow"
slow-timeout = { period = "90s", terminate-after = 2 }

[[profile.default.overrides]]
filter = "package(bijux-dev-atlas) and test(docs_links_and_images_follow_governance_rules)"
slow-timeout = { period = "2s", terminate-after = 12 }

[[profile.default.overrides]]
filter = "package(bijux-dev-atlas) and test(build_bin_writes_manifest_when_effects_enabled)"
slow-timeout = { period = "250ms", terminate-after = 960 }

[store]
dir = "artifacts/isolate/test/nextest-store"

[profile.ci]
retries = { count = 0, backoff = "fixed", delay = "1s" }
fail-fast = false
test-threads = 1
failure-output = "immediate"
success-output = "never"
status-level = "pass"

[profile.fast-unit]
retries = { count = 0, backoff = "fixed", delay = "1s" }
fail-fast = true
test-threads = 8
failure-output = "immediate-final"
success-output = "never"
status-level = "pass"

[profile.slow-integration]
retries = { count = 1, backoff = "fixed", delay = "1s" }
fail-fast = false
test-threads = 4
failure-output = "immediate-final"
success-output = "never"
status-level = "pass"

[profile.certification]
retries = { count = 0, backoff = "fixed", delay = "1s" }
fail-fast = true
test-threads = 1
failure-output = "immediate-final"
success-output = "never"
status-level = "skip"
