# Internal configs

- Owner: `platform`
- Purpose: hold tooling-only config governance notes that must stay outside public config domains.
- Consumers: `bijux-dev-atlas` contracts and maintainers.
- Update workflow: keep this surface minimal and avoid runtime-facing configuration here.
