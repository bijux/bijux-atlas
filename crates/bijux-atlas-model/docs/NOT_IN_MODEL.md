# What Is Not In Model

This crate does not define:
- Store backend implementations.
- SQLite query/runtime code.
- Network/service clients.

Note:
- Artifact path templates are part of artifact contract shape, not runtime store behavior.
