# Realistic Fixture (Non-CI)

This fixture is intentionally larger than tiny test fixtures and is meant for manual local profiling, not CI.

It mimics a small public-genome style subset (GFF3 + FASTA + FAI) with enough rows to inspect ingestion behavior.
