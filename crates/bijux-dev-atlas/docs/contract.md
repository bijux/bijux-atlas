# Contract (Alias)

Canonical contract documentation moved to `control-plane-contracts.md`.

This file remains as a compatibility alias for existing links.

Runtime product CLI namespace: `bijux atlas <args>`
Installed umbrella dispatch: `bijux dev atlas <args>`
Naming contract is frozen
