{
  "schema_version": 1,
  "client_major_support": {
    "0": [
      "0.1."
    ]
  },
  "notes": "Compatibility is evaluated using runtime /version (fallback /health) and semantic version prefix matching."
}

