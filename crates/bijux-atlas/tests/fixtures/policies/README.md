# Policy Fixtures

Regression fixtures for explicit GFF3 policy branches:

- `invalid_strand.gff3`: strand validation (`+/-/.` only)
- `invalid_cds_phase.gff3`: CDS phase validation (`0/1/2/.`)
- `duplicate_transcript_ids.gff3`: duplicate transcript ID behavior
- `unordered_features.gff3`: feature ordering independence
